# INSPIRE overlay fonts

Optional bundled fonts for `overlay_renderer.py` v2.

Drop these files here (preferred order):

- `RobotoCondensed-Bold.ttf`
- `RobotoCondensed-Regular.ttf`

If missing, renderer falls back to Segoe UI / Calibri (Windows) or DejaVu (Linux).
