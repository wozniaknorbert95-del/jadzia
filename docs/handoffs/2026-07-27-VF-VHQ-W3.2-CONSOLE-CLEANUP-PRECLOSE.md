---
status: "[PRECLOSE]"
title: "VF-VHQ-W3.2 — Console Cleanup Pre-Close Report"
updated: "2026-07-27"
gate: "VF-VHQ-W3.2-CONSOLE-CLEANUP"
cache: "vhq-w32a"
dogfood_primary: "http://127.0.0.1:8765/index.html?v=vhq-w32a"
dogfood_legacy: "http://127.0.0.1:8765/index.html?v=vhq-w32a&vhq_shell=legacy"
commit: false
deploy: false
w3_2_closed: false
w4_started: false
recommendation: "READY FOR FOUNDER DOGFOOD"
---

# VF-VHQ-W3.2 — Console Cleanup Pre-Close Report

## Verdict

**READY FOR FOUNDER DOGFOOD**

Local smoke + propagation fixture **PASS**. No commit. No deploy. W4 parked. MKT untouched.

## Manifest contract

`VHQ_ROOMS` in `commander-ui/app.js` is the **sole** room-metadata SoT.

| Field | Role |
|-------|------|
| `id` / `aliases` | Canonical key + legacy id map |
| `label` / `floor` / `purpose` | Identity |
| `status` / `evidence` / `lastVerified` / `owner` / `limitation` | Honesty |
| `sotLabel` / `sotHref` / `action` | SoT + primary action |
| `mvp` / `pulse` / `truthPilot` / `floorCard` | Surface inclusion flags |
| `mapHop` / `mapHops` | System Map hops |
| `flowOrder` | Ops Flow + Sales handoff strip |
| `criticalPin` / `criticalDetail` | SSH pin on Mission Control |
| `briefVault` | Approval Vault strip |
| `honesty` / `kpi` | Work View + Truth Cards |
| `technicalProbe` | Design Agent ≠ Production desk |
| `links` | Safe DOM links (no raw `extraHtml`) |

**Removed:** separate `VHQ_PULSE` array.

Renderers (all read `VHQ_ROOMS` only):

- `vhqRenderPulse` · `vhqRenderFloorCards` · room panel (`vhqRenderRoom`)
- `vhqRenderOpsFlow` · `vhqBindWorkViews` · `vhqRenderCriticalPin` · `vhqRenderVaultStrip`
- `vhqRenderSystemMap` · `vhqRenderSettingsMap` · `vhqRenderTruthCards` · `vhqRenderTeleportOptions`

Propagation fixture: `window.vhqManifestPropagationTest()` — mutates Marketing status temporarily, asserts Pulse/floor/map/Settings/Truth, restores.

## Canonical ID / alias map

| Canonical | Aliases |
|-----------|---------|
| `mission-control` | — |
| `approval-vault` | — |
| `ai-agent-health` | — |
| `sales-room` | `sales-queue` |
| `wizard-quote` | `showroom-wizard` |
| `marketing-studio` | — |
| `order-desk` | `orders-production` |
| `analytics-finance` | `analytics`, `finance-analytics` |
| `compliance-audit` | `compliance` |
| `knowledge-library` | — |
| `vcms-os-zone` | (+ nested `mapHops` Agent OS / VCMS) |
| `supplier-dock` | `supplier-warehouse` |
| `design-agent-probe` | — (`technicalProbe`, no floor card) |

## Removed duplication inventory

| Surface (before) | After |
|------------------|--------|
| Hardcoded floor `data-status` / badges | Generated from `VHQ_ROOMS` |
| `VHQ_PULSE` duplicate array | Derived via `pulse:true` |
| Ops Flow HTML statuses | `flowOrder` render |
| Sales/Wizard handoff + Work meta/honesty | `vhqBindWorkViews()` |
| Critical SSH + Vault strip strings | `criticalPin` / `briefVault` |
| System Map hardcoded hops + mixed ids | `mapHop` / `mapHops` |
| Settings map `<li>` strings | `#settings-system-map` generated |
| Truth Cards static articles | `truthPilot` generated |
| Console as second “home” dashboard | Collapsed Technical + Legacy hosts |

**Scan:** zero `EV-W2-*` / `EV-W3-*` literals in `index.html`. Outside `VHQ_ROOMS` object body in `app.js`: no hardcoded EV status strings on those surfaces (IDs live only in the manifest).

## Console before / after

| | Before | After |
|--|--------|--------|
| Role | Felt like second Director home (map + Truth + ops/queue always visible) | Secondary utility |
| Heading | “Non-dominant fallback…” | “Secondary utility · auth, tickets and evidence. Primary dashboard = Virtual HQ.” |
| First CTA | Return to HQ (present) | **Return to HQ** first primary control (kept) |
| Auth / ticket / undo | Global (kept) | **Kept** |
| System Map + Truth | Always expanded | **`<details>` Technical / Evidence** (closed by default) |
| Ops / priorities / queue / CS | Visible Console sections | **`<details>` Legacy operator hosts** (slot hosts only; closed by default) |
| Audyt / Agenci | Via tabs/Settings | Links inside Technical collapse |

## Propagation test evidence

```text
vhqManifestPropagationTest() → { pass: true, hits: [true,true,true,true,true],
  surfaces: ["pulse","floor","system-map","settings","truth"] }
```

Marketing consistency after restore: Pulse + floor + map + Settings + Truth all show **UNVERIFIED · EV-W3-001**.

## Regression results

| Check | Result |
|-------|--------|
| Primary cold-open → HQ / Mission Control | **PASS** |
| HQ not dialog (`role=null`) | **PASS** |
| Start → MC · 5 tabs | **PASS** |
| Esc room → MC → Console | **PASS** |
| Sign in → Console + JWT focus | **PASS** |
| Return to HQ | **PASS** |
| One `#queue-list` / `#priorities` / CS form | **PASS** (1 each) |
| Marketing UNVERIFIED on all listed surfaces | **PASS** |
| Legacy `?vhq_shell=legacy` modal + Esc restore | **PASS** (dialog, portal BODY, inert, focus `#vhq-enter`) |
| Mobile brief-first (390×844 Navigate HQ) | **PASS** |
| Cache token `vhq-w32a` | **PASS** |
| No 6th tab / no MKT edits / no W4 | **PASS** |

## Exact changed files

```text
commander-ui/app.js
commander-ui/index.html
commander-ui/styles.css
todo.json
docs/handoffs/2026-07-27-VF-VHQ-W3.2-CONSOLE-CLEANUP-PRECLOSE.md
```

```text
git diff --stat (runtime):
 commander-ui/app.js     | ~1058 +++ (manifest + renderers)
 commander-ui/index.html | ~287  --- (strip hardcoded surfaces + Console collapses)
 commander-ui/styles.css | +25   (console-collapse)
```

MKT dirty paths remain unstaged / untouched.

## Founder dogfood URL

```text
Primary: http://127.0.0.1:8765/index.html?v=vhq-w32a
Legacy:  http://127.0.0.1:8765/index.html?v=vhq-w32a&vhq_shell=legacy
```

Optional console check: open Operations Console → confirm Technical/Legacy **collapsed**; expand Technical → Map + Truth from manifest.

## Explicit non-actions

- No commit
- No deploy
- No W3.2 CLOSE
- No W4
- No MKT / Ads / Mollie / new APIs / 6th tab

## Recommended next Founder decision (HITL)

```text
1) Dogfood local W3.2 (primary + legacy + Console collapses)
2) If OK → GO CLOSE VF-VHQ-W3.2
3) Then separate GO: commit (exclude MKT) → deploy
4) W4 remains parked until after prod dogfood + explicit GO
```
