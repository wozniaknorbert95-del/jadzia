# BLAST — COI-CS-02 CS API + UI

**Date:** 2026-07-18  
**Repo:** jadzia-core ONLY  
**Prereq:** COI-CS-01 spawn/queue + UX-03 dogfood PASS  
**Decision:** Next 1-1-1 after Dashboard LIVE — **CS API+UI** (not OPS-AI forge, not meta mirror).

## B — Background

Scorecard #6 PARTIAL: spawn+queue bez Commander surface. CEO nie może ręcznie odpalić post-sale follow-up.

## L — Limits (1-1-1)

- `POST /api/v1/commander/cs/followup` + ticket disposition HITL
- Home form PL (order_id) + queue buttons Potwierdź/Odłóż/Zamknij
- **Nie** auto-email klienta; **nie** helpdesk merge; **nie** OPS-AI; **nie** Gate D

## A — Approach

1. DB `db_commander_update_ticket_status`
2. API spawn + disposition + audit
3. UI Home + Agents LIVE card
4. pytest + prod dogfood spawn→Ack
5. Scorecard #6 LIVE tylko po DoD

## S — STOP

SSO, Agent OS merge, auto-trigger paid orders, Mollie LIVE, fałszywy OPS-AI PASS.

## T — DoD (MUST all PASS)

- [ ] `POST /cs/followup` → ticket `source=cs_followup` in queue ACTION
- [ ] `POST /tickets/{id}/disposition` acked|snoozed|closed removes from open queue
- [ ] Home UI: form + PL buttons on CS card
- [ ] pytest: spawn API + disposition + queue mapping
- [ ] Agents card CS ≠ PARTIAL stub
- [ ] Prod dogfood: 1 spawn → Potwierdź
- [ ] Scorecard #6 → LIVE; PROCESS P-CS-01 COVERED
- [ ] Handoff CLOSE + tip SoT
