## Handoff: A4-01 — SQLite-only Persistence

**Data:** 2026-06-26
**Autor:** Agent

### Co zrobiono

Przejście na **SQLite jako jedyne źródło prawdy** dla stanu sesji/tasków.

| Zmiana | Plik(i) |
|--------|---------|
| Usunięto flagę `USE_SQLITE_STATE` | `agent/state/_config.py`, `__init__.py` |
| `load_state` — tylko SQLite, bez JSON fallback | `agent/state/core.py` |
| `clear_state` — tylko `db_delete_session` | `agent/state/core.py` |
| `cleanup_old_sessions` / `list_active_sessions` — tylko DB | `agent/state/tasks.py` |
| Dashboard i health bez JSON fallback | `api/routes/dashboard.py` |
| Test dashboard: `db_unavailable` zamiast `sqlite_required` | `tests/test_api_integration.py` |
| Deploy script bez `USE_SQLITE_STATE` | `deployment/deploy-to-vps.sh` |
| CI bez env flag | `.github/workflows/tests.yml` |
| Blueprint | `docs/plans/BLUEPRINT-A4-01-sqlite-only.md` |
| Deprecated script → archive | `archive/restore_sessions_from_backup.py` |
| brain.md — SQLite-only note | `brain.md` |

### Bez zmian (świadomie)

- File locks (`data/sessions/.locks/`) — concurrency
- `archive_state()` — JSON do `data/backups/` (backup operacyjny)
- `migrate_legacy_state()` — jednorazowa migracja `.agent_state.json`

### Stan testów

**304 passed, 1 skipped, 1 xfailed**

### Następny krok

**A3-01** — dokończyć migrację `call_claude`, `detect_session_source` z `agent/agent.py` do `core/llm.py`.

**S1-01** — rotacja sekretów (blocked, Dowódca).

### Rollback na VPS

Jeśli na produkcji są sesje tylko w `data/sessions/*.json`: one-shot import przez `_sync_to_sqlite` przed deployem nowej wersji.
