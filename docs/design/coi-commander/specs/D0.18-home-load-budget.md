# D0.18 — Home load budget

**Status:** Spec  
**Date:** 2026-07-18

## Chunk order

1. **Critical path (blocking):** priorities + queue → render first  
2. **Secondary (non-blocking):** agents SLA, analytics freshness, settings/delegat banner  

## Rules

- Do not `await Promise.all` critical+secondary together if secondary failure blocks queue.  
- Show skeleton or keep previous / „Ładowanie…” on priorities/queue until critical returns.  
- Secondary `.catch` → health strip partial message, Home remains usable.
