# D0.9 — ApprovalCard Contract + Escalation Recipient Model

## ApprovalCard contract (CE-07)

```json
{
  "payload": {},
  "escalation_reason": "string",
  "source": "agent|calendar|lead",
  "confidence": 0.0,
  "available_actions": ["approve", "reject", "edit", "defer"]
}
```

## Escalation timeline (CE-03 / N6)

```
pending > 12h  → amber (dashboard)
pending > 24h  → red + push Dowódca
pending > 24h AND Dowódca inactive → push + email Delegat
agent silent > 2× interval → push Dowódca + Delegat
```

## Wymagania prod

- Min. 1 Delegat skonfigurowany w Settings
- Bez Delegata: warning banner + email fallback
