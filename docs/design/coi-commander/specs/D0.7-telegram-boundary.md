# D0.7 — Telegram Boundary (CE-02)

## Dozwolone

- Push alert + signed deep-link
- SLA breach → Dowódca, potem Delegat
- Published confirmation digest
- Dashboard-down health notify
- `/ticket` → queue + link

## Zabronione

- SSH prod (`/zadanie` deprecated → `/ticket`)
- Approve/publish mutations w TG
- Registry / pause agentów
- Session token w URL

## Migracja

`/zadanie` tworzy ticket zamiast SSH task (backward compat message redirects user).
