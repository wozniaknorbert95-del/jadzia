# D0.6 ADR — Phone-first Commander hub (not-merge)

**Status:** Accepted (URL table amended 2026-07-18 — hosted VCMS + Agent OS)  
**Date:** 2026-07-18  
**Supersedes relation:** Complements D0.5 (hosting); does not reverse greenfield SPA.

## Context

Dowódca needs one smartphone entry point for FlexGrafik control. Separate products exist:

| Surface | Role |
|---------|------|
| Jadzia COI Commander | Business ops (leads, marketing, analytics, agents) |
| Agent OS Mission Control | Code orchestration / HITL diffs (`os.flexgrafik.nl`) |
| VCMS Command Center | Governance / repo scan (`cmd.flexgrafik.nl`) |
| Wizard / Design Agent | Customer + mockups |

Merging these codebases would couple auth, deploy risk, and cognitive domains incorrectly.

## Decision

1. **Keep backends separate** (jadzia-core ≠ agent-os ≠ VCMS ≠ zzpackage).
2. **Phone UX hub = Commander** at `/commander/` (same FastAPI unit as D0.5).
3. **System map** on Home: deep-links outward to **hosted** Agent OS, VCMS, Wizard (not merge).
4. **Mobile shell:** sticky bottom nav + touch targets ≥44px; Home queue first.

## Canonical URLs (prod)

| Link | URL |
|------|-----|
| Commander hub | `https://api.zzpackage.flexgrafik.nl/commander/` |
| Agent OS Mission Control | `https://os.flexgrafik.nl` (local-dev: `http://localhost:3000`) |
| VCMS Command Center | `https://cmd.flexgrafik.nl` (docs: `https://cmd.flexgrafik.nl/docs/`) |
| Wizard | `https://zzpackage.flexgrafik.nl/wizard/` |
| Design Agent health | `https://api.zzpackage.flexgrafik.nl/api/v1/design-agent/health` |

**Note:** VCMS and Agent OS are **hosted runtime** on VPS (Basic Auth at each host). Commander deep-links only; it does not proxy or merge those apps. Localhost URLs are for laptop/dev only — never as phone prod map targets.

## Consequences

- One cognitive home for Dowódca without false monolith.
- Agent OS / VCMS remain independent deploy units (own Nginx / Basic Auth).
- Demand path (`REV-DEMAND-*`) stays in jadzia; hub does not block revenue APIs.
- PWA / mobile JWT login is separate (MOBILE-02); not part of this URL amendment.

## Out of scope

- Merge agent-os-ui into commander-ui
- SSO across Basic Auth realms
- Rewrite Marketing/Analytics content views
- Gate D / Mollie / min199
- Configurable map URLs in SQLite settings (future slice)

## Relation to Demand

Demand F0–F7 LIVE; phone hub MOBILE-01/02 LIVE.  
System map prod deep-links: `COI-CMD-MAP-01`.
