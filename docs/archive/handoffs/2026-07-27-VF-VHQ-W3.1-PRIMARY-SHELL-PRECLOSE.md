---
status: "[PRECLOSE]"
title: "VF-VHQ-W3.1-PRIMARY-SHELL — Primary Shell Pre-Close"
updated: "2026-07-27"
gate: "VF-VHQ-W3.1-PRIMARY-SHELL"
cache: "vhq-w31a"
local_url: "http://127.0.0.1:8765/index.html?v=vhq-w31a"
commit: false
deploy: false
w3_1_closed: false
w4_started: false
---

# VF-VHQ-W3.1 — Primary Shell Pre-Close Report

**Verdict: READY FOR FOUNDER DOGFOOD**

## Old vs new navigation

| Step | Before (W3 modal) | After (W3.1 primary) |
|------|-------------------|----------------------|
| Cold-open | Dialog overlay on Home | `#view-hq` / Mission Control (`?vhq=mc`) |
| Start tab | `data-view=home` (Console) | `data-view=hq` → HQ / MC |
| Esc | Close dialog → Home | Ladder B: room → MC → Console |
| Close / Operations Console | Hide dialog | Explicit Console route (`?vhq=console`) |
| Return | Enter Virtual HQ | **Return to HQ** on Console |
| Sign in | Console + JWT | Console + JWT focus (no JWT chrome in HQ) |
| Back/Forward | N/A (modal) | `pushState` transitions; `replaceState` cold-open only |

## Primary-shell evidence

| Check | Result |
|-------|--------|
| `#vhq-shell` `role=dialog` | **removed** (null) |
| `aria-modal` | **removed** (null) |
| Inert backdrop (primary) | **0** inert nodes |
| `#view-hq` default / cold-open | **active** · `homeHidden=true` |
| URL cold-open | `?v=vhq-w31a&vhq=mc` |
| Bottom tabs while HQ open | **visible** (5) |
| Legacy `?vhq_shell=legacy` | dialog + aria-modal + inert restored |

## Canonical DOM ownership

| Node | MC | Sales | Console |
|------|----|-------|---------|
| `#home-ops-rail` | mounted `#vhq-mount-ops` | restored home | `#vhq-home-slot-ops` |
| `#priorities` | mounted | restored | `#vhq-home-slot-priorities` |
| `#queue-list` | mounted | mounted `#vhq-mount-work-queue` | `#vhq-home-slot-queue` |
| `#cs-followup-form` | restored | mounted `#vhq-mount-work-cs` | `#vhq-home-slot-cs` (fallback) |

**Always exactly one `#queue-list`.** Verified `queueCount=1` on MC, Sales, Console.

## URL / history behavior

| Param | Behavior |
|-------|----------|
| `v=` | Preserved (cache `vhq-w31a`) |
| `vhq=mc` | Mission Control |
| `vhq=world` | Floor browse |
| `vhq=sales-room` / `wizard-quote` / `marketing-studio` / other room ids | Room work / panel |
| `vhq=console` | Operations Console |
| Cold-open | **`replaceState`** normalize |
| User transitions | **`pushState`** |
| `popstate` | Restores matching VHQ state |
| `?ticket=` | Console + `vhq=console` (replace) |

Verified: Sales `pushState` → Esc → `vhq=mc` → Esc → `vhq=console` → Return → `vhq=mc`; history.back Sales→MC; Sign in → Console + `jwt-input` focused.

## Rollback proof

| Path | How |
|------|-----|
| Flag OFF | `?vhq_shell=legacy` → modal dialog + inert + trap |
| Flag ON | `data-vhq-primary="1"` (local default) or `?vhq_shell=primary` |
| Prod default | Still requires separate deploy GO (local primary ON for dogfood only) |
| Git | Revert 3 `commander-ui/*` files — no schema |

## Regression tests

| # | Test | Result |
|---|------|--------|
| 1 | Cold-open → HQ / MC | **PASS** |
| 2 | Start → HQ / MC | **PASS** (`data-view=hq`) |
| 3 | room → Esc → MC → Esc → Console | **PASS** |
| 4 | Console → Return to HQ | **PASS** |
| 5 | Sign in → Console → JWT focus | **PASS** |
| 6 | Back/Forward MC ↔ Sales | **PASS** |
| 7 | ticket deeplink → Console | **PASS** |
| 8 | Queue + CS contracts (ids / single queue) | **PASS** |
| 9 | Marketing tab reachable; HQ observe-only EV-W3-001 | **PASS** (unchanged honesty) |
| 10 | Mobile Navigate HQ present | **PASS** (drawer summary) |
| 11 | Five tabs | **PASS** |
| 12 | Primary ON / legacy OFF paths | **PASS** |
| 13 | SSH DEGRADED / PARKED / UNVERIFIED copy | **PASS** preserved |
| 14 | No MKT / Ads / backend / W4 | **PASS** |

## Exact changed files

```text
commander-ui/index.html
commander-ui/app.js
commander-ui/styles.css
docs/handoffs/2026-07-27-VF-VHQ-W3.1-PRIMARY-SHELL-PRECLOSE.md  (this file, uncommitted)
```

## Git diff (stat)

```text
commander-ui/app.js     | +357 / −…   (history, primary shell, Esc ladder, flag)
commander-ui/index.html | shell → #view-hq; Start=hq; Return to HQ; cache vhq-w31a
commander-ui/styles.css | non-modal primary layout; legacy modal CSS retained
```

~597 insertions / 299 deletions across the three UI files (no backend).

## Founder dogfood URL

```text
http://127.0.0.1:8765/index.html?v=vhq-w31a
```

Rollback dogfood:

```text
http://127.0.0.1:8765/index.html?v=vhq-w31a&vhq_shell=legacy
```

Deep links:

```text
…&vhq=mc
…&vhq=world
…&vhq=sales-room
…&vhq=wizard-quote
…&vhq=marketing-studio
…&vhq=console
…&ticket=999
```

## Explicit non-actions

- No commit  
- No deploy  
- No W3.1 CLOSE  
- No W4  
- No MKT / Ads / Mollie / API / infra  

## Recommended next Founder decision

```text
Dogfood local ?v=vhq-w31a
Then either:
  GO VF-VHQ-W3.1-PRIMARY-SHELL CLOSE (+ optional commit)
  or BLOCKED with findings
Deploy remains a separate GO after CLOSE.
```
