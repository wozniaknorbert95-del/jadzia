venv/
.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
*.key
*.pem
data/
!agent/portal_qualification/data/
!agent/portal_qualification/data/*.json
*.lock
!requirements.lock
!uv.lock
*.log
logs/
.vscode/
.idea/
temp_export/
*.tar.gz
*.zip
*.bak
*.backup
data.backup*/
ngrok.exe
scratch/
*.egg-info/
.pytest_cache/
Portfolio*/
# Local dogfood/recovery helpers — never ship
deployment/_mint_*.py
deployment/_recover_*.py
# Local test noise
.coverage
coverage.xml
htmlcov/

tmp/

# Demand OS runtime agent state (per-environment, never committed)
docs/ops/demand-os/set-now/AGENTS-HEARTBEAT.json
docs/ops/demand-os/set-now/GROWTH-EVENTS.jsonl

# VPS-local runtime secrets/outputs — never committable from any host (S5)
secrets/
output/

# Other tools' working files, not repo content (S6)
.superpowers/
assets/
